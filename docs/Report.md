ФИО: Жуков Григорий Михайлович  
**Группа:** ПРИ-0-25/1  

**Тема:** Разработка консольного приложения для обработки JSON-данных сенсоров на C++  

---

## 1. Краткое описание задачи и среды выполнения  

**Задача:**  
Разработать консольное приложение на C++, которое считывает, валидирует и анализирует JSON-файлы с данными сенсоров.  
Программа выполняет статистические расчёты (min, max, среднее, медиана) и ищет аномалии по скользящему окну.  

**Среда выполнения:**  

- Операционная система: Windows 10  
- IDE: Microsoft Visual Studio 2022  
- Компилятор: MSVC (C++17)  
- Библиотеки: nlohmann/json  

---

## 2. Структура проекта и схема данных  

**Структура проекта:**~
[200~SensorTelemetry/
├── src/ # исходный код .cpp
├── include/ # заголовочные файлы .h/.hpp
├── tests/ # самотесты
├── docs/ # отчёт, план реализации, бенчмарки
├── data/ # примеры JSON
├── release/ # готовый .exe
└── .gitignore~
**Схема данных JSON:**
```json
{
    "sensor": "s1",
    "ts": "2025-06-01T00:00:00Z",
    "value": 12.3
}
sensor — имя сенсора (строка)

ts — временная метка в формате ISO 8601

value — числовое значение (0–100)
3. Описание ключевых алгоритмов
Валидация данных:

Проверка наличия полей ts и value

Проверка диапазона значения value (0–100)

Проверка уникальности временных меток ts

Статистический анализ:

Вычисление минимального и максимального значений

Вычисление среднего арифметического

Вычисление медианы

Поиск аномалий по скользящему окну:

Для каждого окна из N точек вычисляется среднее значение

Если текущая точка > 1.5×среднее или < 0.5×среднее, она считается аномальной

Размер окна настраивается пользователем

Замер производительности:

Измерение времени вычисления медианы

Измерение времени анализа скользящих окон
4. Выдержки из интерфейсов функций и примеры использования
json readJsonFile(const string& filename);
void validateData(const json& data);
void analyzeSensorData(const json& data, const string& sensorFilter, int windowSize, bool timeCheck);
Пример использования:
json data = readJsonFile("data_0.json");
validateData(data);
analyzeSensorData(data, "s1", 20, true);
5. Обработка ошибок и UX взаимодействие
Обработка ошибок:

Отсутствие файла: "Отсутствует JSON отчет. Создайте JSON файл с названием: data_fnumber.json в папке с программой."

Некорректное значение или дубликат ts: выводится сообщение с указанием проблемного значения

Пользовательский интерфейс:

Консольное меню с опциями:

Загрузить и проанализировать файл

Сгенерировать тестовые данные

Запустить валидацию

Режим отладки

Выйти
6. Результаты тестов
Позитивные сценарии:
JSON-файл корректный → данные валидны, статистика и аномалии вычисляются корректно

Негативные сценарии:
Пропуск поля ts → выводится предупреждение

Дублирование ts → выводится предупреждение

value вне диапазона 0–100 → выводится предупреждение
7. Результаты бенчмарков
Файлов проверено	Время анализа (мс)	Размер окна	Примечания
100	6680	20	Линейная зависимость
1000	54181	20	Линейная зависимость
10000	514324	20	Прогноз для 100000: ~5 млн мс (~83 мин)
Полный отчет доступен в файле: docs/benchReport.md
8. Выводы и предложения по улучшению
Выводы:

Программа корректно работает с JSON-форматом

Статистические показатели вычисляются верно

Алгоритм поиска аномалий функционирует согласно требованиям

Подтверждена линейная зависимость времени анализа от количества файлов

Предложения по улучшению:

Оптимизация производительности:

Использование скользящей суммы/среднего вместо пересчёта каждого окна заново

Многопоточная обработка нескольких файлов одновременно

Кэширование результатов валидации

Расширение функционала:

Добавление возможности выбора формата вывода (JSON, CSV, XML)

Реализация фильтров по датам и сенсорам

Поддержка батч-обработки множества файлов

Улучшение UX:

Графический интерфейс пользователя

Интерактивная визуализация данных и аномалий

Возможность экспорта отчетов в PDF

Расширение аналитических возможностей:

Реализация машинного обучения для обнаружения сложных аномалий

Прогнозирование значений на основе исторических данных

Корреляционный анализ между разными сенсорами

Заключение:
Программа успешно решает поставленные задачи, обладает модульной архитектурой и может быть расширена для решения более сложных задач анализа данных телеметрии.
